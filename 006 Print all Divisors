from math import sqrt
class Solution:
    def divisors(self, n):
        div=[]
        for i in range(1,int(sqrt(n)+1)):
            if n%i==0:
                div.append(i)
                if n/i!=i:
                    div.append(int(n/i))
        return sorted(div)
