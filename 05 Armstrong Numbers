class Solution:
    def isArmstrong(self, n):
        dup=n
        p=int(len(str(n)))
        sum=0
        while n>0:
            last_digit=n%10
            sum=sum+pow(last_digit,p)
            n=n//10
        if sum==dup:
            return True
        else:
            return False
